checkpoint_path: checkpoints/neurogrip_best.ckpt
output_path: models/neurogrip_6g.mindir
input_shape: [1, 12, 24, 6]
mindir_version: latest

model:
  in_channels: 12
  num_classes: 6
  hidden_dim: 64
  num_layers: 2
  dropout_rate: 0.0

